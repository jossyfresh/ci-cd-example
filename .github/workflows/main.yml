name: Test Suite

on:
  push:
    branches:
      - josyy

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        run: pytest
  deploy:
    name: Deploy to Docker Hub
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        run: echo "dckr_pat_I7VWrvkLc3Z_uQOp3w-rvpHueEQ" | docker login -u jossyfresh2002425 --password-stdin
  
      - name: Build and push Docker image
        run: |
          docker build -t jossyfresh2002425/dockerlesson-image .
          docker push jossyfresh2002425/dockerlesson-image

